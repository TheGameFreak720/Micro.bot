extends layout

block content
    h1 #{title}
    ul.list-group
        each post, i in posts
            li.list-group-item
                a(href="/posts/" + post._id)= post.body
block script
    script(src='/js/inactivity.js')